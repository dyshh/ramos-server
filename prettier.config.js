'use strict';
module.exports = {
    // tab缩进大小,默认为2
    tabWidth: 4,
    // 使用分号, 默认true
    semi: true,
    trailingComma: 'none',
    // 使用单引号, 默认false(在jsx中配置无效, 默认都是双引号)
    singleQuote: true
};
